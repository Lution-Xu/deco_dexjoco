# 项目理解与部署文档

## 1. 项目总体概述

这个项目是 **DECO: Decoupled Multimodal Diffusion Transformer for Bimanual Dexterous Manipulation with a Plugin Tactile Adapter** 的训练与部署代码。核心目标是做机器人双臂灵巧操作的模仿学习：从示教数据中学习策略，让模型根据双目/双相机图像、机器人本体状态、可选触觉数据，预测未来一段动作序列。

项目输入主要是：

- 两路 RGB 图像：`img1`、`img2`，来自左/右相机、头部双目或 LIBERO 仿真相机。
- 机器人状态：双臂、双手、头部云台等关节状态。
- 可选触觉：左右 Inspire Hand 触觉阵列，各 `1062` 维。
- 可选任务条件：`task_idx`，用于多任务 one-hot / embedding 条件。

项目输出主要是：

- 一个动作 chunk，例如 DECO 默认输出 `[chunk_size=32, action_dim=28]`。
- 实机部署时，动作被拆成左臂、左手、右臂、右手、头部云台控制量。
- LIBERO 评估时，动作是 7 维机械臂末端/夹爪动作。

它属于 **机器人模仿学习训练 + 策略推理 + 实机部署 + 仿真评估** 项目，不是纯感知项目。通俗地说：先把示教数据整理成“图像 + 当前状态 → 未来动作”的样本，用 DECO/ACT/DP 训练策略；推理时读取当前相机和机器人状态，模型预测未来动作序列，再把动作发给 Unitree 机器人、Inspire 灵巧手和 Dynamixel 主动相机。

## 2. 项目目录结构说明

| 路径 | 类型 | 主要作用 | 是否核心 |
| -- | -- | -- | -- |
| `README.MD` | 文档 | 安装、数据、训练、部署、LIBERO 简要说明 | 文档 |
| `train.py` | Python | 训练主入口，加载 YAML、数据集、模型、优化器、DDP | 核心 |
| `dataset.py` | Python | DECO-50 / 自定义真实机器人数据加载 | 核心 |
| `libero_dataset.py` | Python | LIBERO 转换后数据加载 | 核心 |
| `inference.py` | Python | 推理预处理、后处理、模型构建、动作平滑 | 核心 |
| `eval_libero.py` | Python | LIBERO 仿真评估入口，统计成功率并保存视频 | 核心 |
| `config/` | 配置 | ACT、DP、DECO、DECO-LIBERO 的 YAML 配置 | 配置 |
| `models/deco/` | Python | DECO 模型、训练/验证、RoPE、去噪时间表 | 核心 |
| `models/act/` | Python | ACT baseline 模型和训练逻辑 | 核心 |
| `models/dp/` | Python | Diffusion Policy baseline 模型和训练逻辑 | 核心 |
| `models/pi05/` | Python | Pi0.5/Gemma 相关实验代码；当前训练入口未直接配置使用 | 暂不确定 |
| `deploy/deploy_h1.py` | Python | H1-2 实机部署主入口 | 核心 |
| `deploy/robot_control/` | Python | Unitree 机械臂、灵巧手、IK、重定向控制 | 核心 |
| `deploy/dynamixel/` | Python | Dynamixel 主动相机云台驱动 | 核心 |
| `deploy/teleimager/` | Python 包 | 图像服务器/客户端，ZMQ/WebRTC，支持 ZED/UVC/RealSense | 核心 |
| `deploy/televuer/` | Python 包 | XR/遥操作可视化接口，实机部署辅助 | 辅助 |
| `deploy/utils/` | Python | 平滑滤波、IPC、运动模式切换 | 辅助 |
| `assets/` | URDF/STL/YAML | Unitree H1/H1-2/G1、灵巧手、主动相机模型和配置 | 数据 |
| `utils/` | Python | 数据合并、统计均值方差、LIBERO 格式转换 | 辅助 |
| `requirements.txt` | 配置 | 主训练依赖 | 配置 |
| `requirements_libero.txt` | 配置 | LIBERO 仿真评估依赖 | 配置 |
| `LICENSE` | 文档 | Apache 2.0 | 文档 |

判断依据：`train.py` 通过 `importlib.import_module(f"models.{model_name}")` 动态加载 `models/deco|act|dp`，部署入口 `deploy_h1.py` 直接调用 `teleimager`、`robot_arm`、`robot_hand_inspire_deploy`、`dynamixel` 和 `inference.py`。

## 3. 项目运行流程总览

主训练入口是 `train.py`。

训练流程：

```text
解析 argparse 参数
→ 设置 CUDA_VISIBLE_DEVICES / 初始化 DDP
→ 读取 YAML 配置
→ 根据 model_name 动态导入 models.deco / models.act / models.dp
→ 构建图像 transform
→ 构建 my_Dataset 或 libero_Dataset
→ 构建 DataLoader
→ 初始化模型
→ 初始化 AdamW/SGD、CosineAnnealingLR、WarmUpLR
→ 可选 resume
→ epoch 循环
→ importmodule.train()
→ importmodule.val()
→ 保存 best.pth / last_weights.pth / epoch_xxx.pth
→ 写 result.txt 和 loss 曲线
```

DECO-50 数据读取来自 `dataset.py`。LIBERO 数据读取来自 `libero_dataset.py`。

实机推理部署入口是 `deploy/deploy_h1.py`。

实机流程：

```text
读取 YAML 和权重路径
→ modeling() 构建策略模型
→ ImageClient 连接图像服务器
→ Unitree DDS 初始化手臂控制器
→ Inspire Hand DDS 初始化手和触觉
→ Dynamixel 串口初始化主动相机
→ 从示教 JSON 取初始姿态
→ 等待键盘 r 启动
→ 循环读取头部双目图像、机器人状态、触觉
→ predict_action()
→ receding horizon 或 temporal ensemble 选动作
→ 控制双臂、双手、主动相机
→ 按 fps 睡眠
```

LIBERO 评估入口是 `eval_libero.py`。

```text
读取 eval_libero.py 内部 Config
→ 加载 YAML 和模型
→ 加载 tasks.json
→ 初始化 LIBERO benchmark/env
→ 每个任务多次 rollout
→ predict_action()
→ env.step(action)
→ 统计 success rate
→ 保存 MP4 和 txt 日志
```

## 4. 核心模块拆解

### `train.py`

**功能定位：** 统一训练入口，支持 DECO、ACT、DP 三种模型。

**主要类和函数：**

- `WarmUpLR`：warmup 学习率调度。
- `LossHistory`：保存 `train_loss.txt`、`val_loss.txt` 和 loss 曲线。
- `main(opt)`：完整训练流程。

**输入与输出：**

- 输入：YAML 配置、数据目录、训练参数。
- 输出：`best.pth`、`last_weights.pth`、周期 checkpoint、`result.txt`、loss 图片。

**被谁调用：** 命令行直接运行，或 `torchrun` 启动。

**我需要重点理解的代码段：**

- `config = yaml.safe_load(open(opt.config, 'r'))`：配置加载。
- `importlib.import_module(f"models.{model_name}")`：模型动态导入。
- `my_Dataset` / `libero_Dataset`：数据集选择。
- epoch 循环中的 `importmodule.train()` 和 `importmodule.val()`。

### `dataset.py`

**功能定位：** 真实机器人/DECO-50 数据集加载。

**主要类和函数：**

- `letterbox`：等比例缩放并 padding 到正方形。
- `my_Dataset`：读取 episode、图像、触觉、状态、动作。

**输入与输出：**

- 输入：`episode_xxxxxx/colors/*.jpg`、`tactiles/*.npy`、`data.pkl`。
- 输出 batch：`img1, img2, tact1, tact2, obs_state, action_padd, mask, task_idx`。

**被谁调用：** `train.py`。

**我需要重点理解的代码段：**

- 触觉路径生成：`*_color_0.jpg` → `*_left_ee_tactile.npy`。
- 状态拼接：`left_obs + right_obs + head_obs`。
- 动作拼接：`left_action + right_action + head_action`。
- `mean_std` 或 `min_max` 归一化。

### `inference.py`

**功能定位：** 训练外推理工具，被实机部署和 LIBERO 评估复用。

**主要类和函数：**

- `preprocess()`：图像、状态、触觉归一化。
- `postprocess()`：动作反归一化。
- `predict_action()`：模型前向推理并输出动作 chunk。
- `modeling()`：根据 YAML 动态构建模型。
- `ACTTemporalEnsembler`：在线动作平滑。

**输入与输出：**

- 输入：numpy 图像、obs、tactile、YAML。
- 输出：反归一化后的动作序列 `[chunk_size, action_dim]`。

**被谁调用：** `deploy_h1.py`、`eval_libero.py`。

**我需要重点理解的代码段：** `predict_action()` 是推理主函数。

### `models/deco/deco.py`

**功能定位：** DECO 模型主体，双图像编码 + 动作去噪 + 可选触觉 adapter。

**主要类和函数：**

- `DECO`：主模型。
- `MMAttention`：图像 token 和动作 token 的联合注意力，可叠加触觉 cross attention。
- `PI_Adapter`：低秩插件 adapter。
- `modeling()`：构建模型和加载权重。

**输入与输出：**

- 训练输入：`img1/img2 [B,3,H,W]`、`obs [B,28]`、`act [B,chunk,28]`、可选 `tac1/tac2`。
- 训练输出：`act_velocity/noise_target`。
- 推理输出：动作 chunk。

**被谁调用：** `train.py` 动态导入；`inference.py` 推理构建。

**我需要重点理解的代码段：**

- `img_encoding()`：双图像特征编码。
- `forward(training=True)`：训练时加噪。
- `forward(training=False)`：推理时迭代去噪。
- `MMAttention.forward()`：图像与动作 token 融合。

### `models/deco/train_one_epoch.py`

**功能定位：** DECO 的训练/验证循环。

**主要类和函数：**

- `train()`：MSE 学习 `noise - action`。
- `val()`：推理生成动作后与 GT 做 L1 loss / MAE。

**输入与输出：**

- 输入：DataLoader batch。
- 输出：epoch loss，checkpoint 由 `train.py` 统一保存。

**我需要重点理解的代码段：**

- `loss = F.mse_loss(out, noise - action)`：DECO 训练目标。
- 验证阶段 `training=False` 后使用 masked L1 loss。

### `models/act/ACT.py`

**功能定位：** ACT baseline，ResNet18 + Transformer Encoder/Decoder + 可选 VAE latent。

**主要类和函数：**

- `ACT`：模型主体。
- `ACTEncoder`、`ACTDecoder`：Transformer 模块。
- `modeling()`：构建模型和加载权重。

**输入与输出：**

- 训练输出：动作序列和 VAE 参数。
- 推理输出：动作序列。

**注意：** `models/act/train_one_epoch.py` 非 AMP 分支引用了不存在的 `opt.action_size`，这是潜在 bug。

### `models/dp/diffusion_policy.py`

**功能定位：** Diffusion Policy baseline。

**主要类和函数：**

- `DiffusionModel`：图像编码 + 条件 1D U-Net。
- `ImageEncoder`：ResNet18 + SpatialSoftmax。
- `ConditionUnet1d`：动作序列去噪网络。

**输入与输出：**

- 训练输出：预测噪声。
- 推理输出：经 DDPM/DDIM scheduler 采样得到的动作 chunk。

**我需要重点理解的代码段：** `DiffusionModel.forward()` 中训练分支学习噪声，推理分支使用 scheduler 逐步去噪。

### `deploy/deploy_h1.py`

**功能定位：** Unitree H1-2 实机部署主程序。

**主要类和函数：**

- `load_episode_actions()`：从示教 JSON 读取初始动作。
- `on_press()`：键盘控制，`r` 启动，`q` 停止，`s` reset。
- `main()`：模型、相机、手臂、手、云台初始化和控制循环。

**输入与输出：**

- 输入：图像服务器帧、机器人 DDS 状态、触觉、YAML 权重。
- 输出：DDS 机械臂/灵巧手命令、Dynamixel 云台命令、可选视频。

**我需要重点理解的代码段：**

- 实机控制主循环。
- `predict_action()` 调用。
- 动作切片并发送到手臂、灵巧手和头部云台。

## 5. 关键数据流分析

### 机器学习数据流

DECO-50 / 自定义数据格式由 README 和 `dataset.py` 共同定义：

```text
Dataset/
├── episode_000000/
│   ├── colors/
│   │   ├── 000_color_0.jpg
│   │   ├── 000_color_1.jpg
│   ├── tactiles/
│   │   ├── 000_left_ee_tactile.npy
│   │   ├── 000_right_ee_tactile.npy
│   └── data.pkl
```

`data.pkl` 需要包含：

- `left_action`
- `right_action`
- `head_action`
- `left_obs`
- `right_obs`
- `head_obs`
- 可选 `condition_indx`

`dataset.py` 的 batch 组成：

| 字段 | 形状/含义 |
| -- | -- |
| `img1` | `[B,3,H,W]`，`*_color_0.jpg` |
| `img2` | `[B,3,H,W]`，`*_color_1.jpg` |
| `tact1` | `[B,1062]`，左手触觉 |
| `tact2` | `[B,1062]`，右手触觉 |
| `obs_state` | `[B,28]`，左状态 + 右状态 + 头部状态 |
| `action_padd` | `[B,chunk_size,28]` |
| `mask` | `[B,chunk_size]`，标记真实动作长度 |
| `task_idx` | `[B]` |

DECO loss：

- 训练时随机采样 `t`，把动作和高斯噪声混合。
- 模型输出目标是 `noise - action`。
- 验证时直接生成动作 chunk，再与真实动作做 masked L1。

训练结果保存：

- `logs/result.txt`
- `logs/loss/train_loss.txt`
- `logs/loss/val_loss.txt`
- `logs/best.pth`
- `logs/last_weights.pth`
- `logs/epoch_xxx_loss_xxx.pth`

### 实机数据流

| 数据 | 来源 | 代码依据 |
| -- | -- | -- |
| 头部双目图像 | `teleimager.image_client.ImageClient` ZMQ | `deploy_h1.py` |
| 手臂状态 | Unitree DDS `rt/lowstate` | `robot_arm.py` |
| 灵巧手状态 | Inspire DDS state topic | `robot_hand_inspire_deploy.py` |
| 触觉 | Inspire DDS touch topic | `robot_hand_inspire_deploy.py` |
| 云台状态 | Dynamixel 串口 `/dev/ttyUSB0` | `deploy_h1.py` |

控制指令发送方式：

- Unitree 手臂：DDS `rt/lowcmd` 或 `rt/arm_sdk`。
- Inspire Hand：DDS `rt/inspire_hand/ctrl/l`、`rt/inspire_hand/ctrl/r`。
- 主动相机：Dynamixel SDK 串口命令。
- 相机图像：ZMQ/WebRTC，不依赖 ROS。

安全边界：

- `H1_2_ArmController.clip_arm_q_target()` 限制关节速度。
- `speed_gradual_max()` 逐步放开速度上限。
- 躯干腰部关节被锁到 0。
- 灵巧手命令被裁剪到 `[0,1000]`。
- 但模型输出动作本身没有统一的 workspace / joint limit safety layer，实机前必须加小范围测试。

## 6. 配置文件与参数说明

| 参数名 | 所在文件 | 默认值 | 作用 | 修改建议 | 修改风险 |
| -- | -- | -- | -- | -- | -- |
| `model_name` | `config/*.yaml` | `deco/act/dp` | 决定导入哪个模型包 | 换模型时修改 | 必须和 `models/` 包名一致 |
| `model.action_dim` | YAML | DECO: `28`，LIBERO: `7` | 动作维度 | 换机器人/任务时必改 | 与数据、归一化长度不一致会报错 |
| `model.chunk_size` | YAML | DECO: `32` | 预测动作序列长度 | 影响闭环频率和训练目标 | 太大训练难、延迟大 |
| `model.obs_state` | YAML | DECO: `True` | 是否使用本体状态 | LIBERO README 建议 False | 与数据统计维度必须匹配 |
| `model.use_tactile` | YAML | `False` | 是否使用触觉 | 有真实触觉时开启 | 无触觉文件会加载失败 |
| `model.use_task_condition` | YAML | `False` / LIBERO `True` | 是否使用任务 ID embedding | 多任务训练开启 | `num_tasks` 必须覆盖 task_idx |
| `model.num_tasks` | YAML | `10` / LIBERO `40` | 任务 embedding 数量 | 多任务改 | task_idx 越界会报错 |
| `model.inf_step` | `deco.yaml` | `5` | DECO 推理去噪步数 | 速度/质量折中 | 过大推理慢 |
| `model.pretrain_model_path` | YAML | `False` | 加载模型权重 | 训练后推理/部署必须填 | 路径错会失败 |
| `model.adapter_model_path` | `deco.yaml` | `False` | 插件 tactile adapter 权重 | plugin 推理时填 | strict load 要结构一致 |
| `data.norm_type` | YAML | `mean_std` | 归一化方式 | 通常保持 | 与统计字段不一致会错 |
| `data.observation_mean/std` | YAML | 数组 | 状态归一化 | 换数据集必须重算 | 维度/分布错导致模型失效 |
| `data.action_mean/std` | YAML | 数组 | 动作归一化 | 换数据集必须重算 | 实机动作幅值会严重错误 |
| `data.tac_left_max/right_max` | YAML | `3486/4050` | 触觉归一化 | 换传感器/数据重算 | 触觉尺度错 |
| `img.img_size` | YAML | `[256,256]` | 图像输入尺寸 | 与训练一致 | 影响 backbone feature 和 RoPE |
| `img.img_mean/std` | YAML | ImageNet | 图像归一化 | 通常保持 | 改错会影响视觉特征 |
| `--config` | `train.py` | `./config/deco.yaml` | 训练配置路径 | 训练不同模型时改 | 路径错 |
| `--data` | `train.py` | `./data` | 数据路径 | 必改 | 数据格式错会加载失败 |
| `--batch-size` | `train.py` | `1024` | 总 batch size | 显存不足时调小 | README 写成 `--batch_size`，代码不认 |
| `--lr` | `train.py` | `1e-4` | 初始学习率 | 常调 | 太大不稳定 |
| `--lr_f` | `train.py` | `5e-6` | cosine 最小学习率 | 常调 | 太小收敛慢 |
| `--epochs` | `train.py` | `200` | 训练轮数 | 数据越大越高 | 过少欠拟合 |
| `--logs` | `train.py` | `./logs/log_dp` | 日志/权重目录 | 必改 | 已存在会自动追加随机数 |
| `--device_id` | `train.py` | `0..7` | CUDA 可见设备 | 按机器修改 | 格式不当影响 DDP |
| `Config.yaml_path` | `eval_libero.py` | 硬编码 `/root/...` | LIBERO 模型配置 | 必改 | 当前默认不可复现 |
| `--network-interface` | `deploy_h1.py` | `enx9c69d30201e2` | Unitree DDS 网卡 | 实机必改 | 选错无法通信 |
| `--img-server-ip` | `deploy_h1.py` | `192.168.123.167` | 图像服务器 IP | 实机必改 | 取不到图像 |
| `--fps` | `deploy_h1.py` | `30` | 策略控制循环频率 | 与相机/机器人匹配 | 过高会延迟积累 |
| `--yaml` | `deploy_h1.py` | `./config/deco.yaml` | 部署 YAML | 从 `deploy/` 运行需用 `../config/deco.yaml` | README 命令和默认路径不完全一致 |
| `--json_path` | `deploy_h1.py` | `~/DECO-50/...` | 初始姿态 JSON | 必改为真实绝对路径 | 代码未展开 `~`，默认会失败 |
| `/dev/ttyUSB0` | `deploy_h1.py` | 硬编码 | Dynamixel 端口 | 按设备改 | 权限/端口错会失败 |
| `cam_config_server.yaml` | teleimager | ZED 1080p/30 | 相机服务器配置 | 实机相机必改 | 分辨率与部署切图逻辑相关 |

后续微调最常改：`data/action_*`、`data/observation_*`、`model.chunk_size`、`model.use_tactile`、`model.pretrain_model_path`、`--data`、`--logs`、`--lr`、`--batch-size`、部署网卡/IP/串口/FPS。

## 7. 环境依赖与安装说明

README 说明该项目测试于：

- Python `3.10`
- PyTorch `2.6`
- CUDA `12.4`

主训练依赖见 `requirements.txt`：

```text
numpy, pandas, scipy, torch, torchvision, transformers, diffusers,
Pillow, PyYAML, einops, tqdm, huggingface-hub, matplotlib
```

LIBERO 额外依赖见 `requirements_libero.txt`：

```text
mujoco, hydra-core, opencv-python, robomimic, robosuite, bddl, gym 等
```

部署依赖是从 import 和 pyproject 推断的，README 没有集中列全：

- `unitree_sdk2py`
- `inspire_sdkpy`
- `dynamixel_sdk`
- `logging_mp` / `logging-mp`
- `sshkeyboard`
- `opencv-python`
- `pyzmq`
- `casadi`
- `pinocchio` / `pin`
- `meshcat`
- `pyzed.sl`，需要 ZED SDK
- `pupil-labs-uvc`
- `aiohttp`、`aiortc`
- 可选 `vuer[all]`
- 可选 `dex_retargeting`

推荐训练环境安装：

```bash
cd /home/xz/deco/IL_training_codebase-master
conda create -n deco python=3.10 -y
conda activate deco
pip install -r requirements.txt
```

下载 DECO-50：

```bash
hf download BAAI-Humanoid/DECO-50 --repo-type dataset --local-dir ./BAAI-Humanoid/DECO-50
```

准备数据：

```bash
python utils/merge.py
python utils/cal_mean_std.py --data-path ./Deco-50/task1_merged --save-path ./Deco-50/task1_statistics
```

注意：`utils/merge.py` 内部路径是硬编码 `/root/total_data/...`，直接运行前必须改路径。

验证安装：

```bash
python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
python -c "import yaml, pandas, diffusers, einops; print('ok')"
```

LIBERO 安装：

```bash
pip install -r requirements_libero.txt
git clone https://github.com/Lifelong-Robot-Learning/LIBERO.git
cd LIBERO
pip install -e .
```

README 还要求 patch LIBERO：把 `torch.load(init_states_path)` 改成 `torch.load(init_states_path, weights_only=False)`。

## 8. 训练 / 推理 / 测试运行方法

### 8.1 训练

推荐使用 DDP，即使单卡也建议 `torchrun --nproc_per_node 1`，因为当前 `models/*/train_one_epoch.py` 里直接调用了 `dist.barrier()` 和 `dist.all_reduce()`。

DECO 多卡训练：

```bash
cd /home/xz/deco/IL_training_codebase-master
torchrun --nproc_per_node 8 train.py \
  --distributed True \
  --amp True \
  --device_id '0,1,2,3,4,5,6,7' \
  --config ./config/deco.yaml \
  --data ./Deco-50/task1_merged \
  --lr 1e-4 \
  --lr_f 5e-6 \
  --batch-size 1024 \
  --num-workers 16 \
  --epochs 200 \
  --logs ./logs/deco_task1 \
  --save_period 10
```

关键注意：

- 代码参数是 `--batch-size`，不是 README 示例里的 `--batch_size`。
- `argparse type=bool` 有坑，`--distributed False` 会被解析为 `True`，因为 `bool("False") == True`。
- 当前单进程非分布式训练存在 `dist.barrier()` 风险，建议先用 `torchrun`。

判断训练正常：

- `result.txt` 持续写入 loss。
- `logs/loss/train_loss.png` 更新。
- GPU 显存被占用。
- `best.pth` 或 `last_weights.pth` 被保存。

### 8.2 推理

项目没有独立的普通离线推理 CLI，推理函数在 `inference.py`。

使用方式是：

```python
import yaml, torch
from inference import modeling, predict_action

yaml_config = yaml.safe_load(open("./config/deco.yaml"))
yaml_config["model"]["pretrain_model_path"] = "./logs/deco_task1/best.pth"
model = modeling(yaml_config).cuda().eval()

actions = predict_action(model, torch.device("cuda"), yaml_config, img1, img2, obs, tac1=left_tac, tac2=right_tac)
```

输出 `actions` 是反归一化后的 `[chunk_size, action_dim]`。

### 8.3 测试或评估

LIBERO 评估：

```bash
export MUJOCO_GL=egl
python eval_libero.py
```

但需要先改 `eval_libero.py` 的 `Config`：

- `yaml_path`
- `task_config`
- `task_suite_name`
- `num_trials_per_task`
- `local_log_dir`

评估指标：

- 每个 task 成功率。
- 总成功率。
- 每条 rollout 保存 MP4。

项目没有发现标准单元测试或 pytest 流程。

## 9. 实机部署分析

### 9.1 主机准备

建议主机：

- Ubuntu Linux。
- Python 3.10。
- NVIDIA GPU，推理至少需要能稳定跑 PyTorch；训练建议多卡 A100 级别，README 使用 8 张 A100 40GB。
- CUDA 12.4 / PyTorch 2.6 对齐。
- 有线网口连接 Unitree 机器人。
- USB 连接 Dynamixel 主动相机，默认 `/dev/ttyUSB0`。
- 能访问图像服务器 IP，例如 `192.168.123.167`。
- 安装 Unitree SDK、Inspire SDK、Dynamixel SDK、ZED SDK、teleimager。

### 9.2 设备准备

需要：

- Unitree H1-2 或代码适配的 G1/H1。
- Inspire Hand 双手及其 DDS server。
- ZED 或其他双目/双相机。
- Dynamixel 舵机主动相机，ID 1 yaw、ID 2 pitch。
- 正确 URDF：`assets/h1_2/h1_2.urdf`。
- 标定好的相机安装位置和云台零点。
- 急停、限位、空旷测试空间。

### 9.3 部署流程

1. 在机器人端启动图像服务器：

```bash
cd deploy/teleimager
pip install -e .
python -m teleimager.image_server
```

2. 在机器人端启动 Inspire Hand server，README 给的是：

```bash
cd inspire_hand_ws
python inspire_hand_sdk/example/Headless_driver_double.py
```

3. 在主机端确认网卡：

```bash
ifconfig
```

4. 修改 `config/deco.yaml`：

```yaml
model:
  pretrain_model_path: /abs/path/to/best.pth
```

5. 修改部署参数：

```bash
cd /home/xz/deco/IL_training_codebase-master
python deploy/deploy_h1.py \
  --network-interface enx9c69d30201e2 \
  --img-server-ip 192.168.123.167 \
  --yaml ./config/deco.yaml \
  --json_path /abs/path/to/episode_0000/data.json \
  --fps 30
```

6. 程序启动后按：

- `r`：启动策略。
- `s`：切换 reset 模式。
- `q`：停止。

### 9.4 实机运行风险

| 风险 | 排查方法 | 建议 |
| -- | -- | -- |
| 坐标/动作顺序不一致 | 对比 `dataset.py` 拼接和 `deploy_h1.py` 切片 | 先离线打印动作含义，再小幅度上机 |
| JSON 初始动作顺序与模型动作顺序不同 | `load_episode_actions()` 与 reset 代码顺序不同 | 明确转换函数，不要手写散落切片 |
| 相机左右图切反 | 查看 `img1/img2 = head_img[:, :W/2]` | 用标定板确认左右图 |
| 控制频率过高 | 记录 loop_time | 从 5-10 Hz 小范围测试 |
| 机器人越界 | 观察关节角和速度 | 增加动作 clip / workspace limit |
| Dynamixel 端口错误 | `ls /dev/ttyUSB*` | 使用 `/dev/serial/by-id` |
| DDS 网卡错误 | Unitree topic 无数据 | 改 `--network-interface` |
| 触觉尺度错误 | 打印触觉 min/max | 重算 `tac_left_max/right_max` |
| 模型输出抖动 | 可视化 action 序列 | 调低 `select-action` 或启用平滑 |
| 权重/配置不匹配 | load_state_dict 报错 | 保证 YAML 与训练时一致 |

## 10. 微调与二次开发指南

### 10.1 数据层面微调

更换数据集时必须保持：

- `colors/*_color_0.jpg` 和 `*_color_1.jpg` 命名规则。
- `tactiles/*_left_ee_tactile.npy` 和 `*_right_ee_tactile.npy`，如果不用触觉也要改 `dataset.py` 或提供 dummy。
- `data.pkl` 中每帧有动作和状态字段。
- 重新运行 `utils/cal_mean_std.py` 生成统计量，并复制到 YAML。

### 10.2 模型或算法层面微调

可改位置：

- DECO：`num_attn_blocks`、`heads`、`dim`、`inf_step`、`use_tactile`、`plugin`。
- ACT：VAE encoder 层数、encoder/decoder 层数。
- DP：`train_step`、`infer_step`、`dim_list`、`spatial_keypoints`。
- Loss：各模型的 `models/*/train_one_epoch.py`。

修改模型结构后，需要同步调整：

- YAML 参数。
- `pretrain_model_path` 权重兼容性。
- 数据维度和归一化统计。
- 部署切片逻辑。

### 10.3 参数层面微调

| 参数 | 调大影响 | 调小影响 |
| -- | -- | -- |
| `chunk_size` | 更长动作规划，延迟更大 | 更闭环，可能更抖 |
| `inf_step` / `infer_step` | 推理质量可能更好，速度变慢 | 更快，动作质量可能下降 |
| `lr` | 收敛快但易炸 | 稳定但慢 |
| `batch-size` | 梯度稳定，显存高 | 显存低，噪声大 |
| `select-action` | 更 open-loop，延迟少 | 更频繁重规划 |
| `fps` | 响应快，负载高 | 稳定但慢 |
| `arm_velocity_limit` | 动作快，风险高 | 更安全但可能跟不上 |

### 10.4 实机部署层面微调

- 相机参数：改 `deploy/teleimager/cam_config_server.yaml`。
- 机器人 IP：图像用 `--img-server-ip`，DDS 主要用 `--network-interface`。
- 串口：`deploy_h1.py` 里 `/dev/ttyUSB0` 当前硬编码。
- 控制频率：`--fps`。
- 运动范围：建议在 `predict_action()` 后、发送控制前增加 action clip。
- 小范围测试：先固定手臂，只测图像和模型输出；再只测手；最后低频率测全链路。

## 11. 代码调用关系图

训练调用链：

```text
train.py
→ yaml.safe_load()
→ importlib.import_module("models.{model_name}")
→ my_Dataset / libero_Dataset
→ DataLoader
→ importmodule.modeling()
→ importmodule.train()
→ importmodule.val()
→ torch.save()
```

DECO 模型链：

```text
models.deco.modeling()
→ DECO()
→ forward()
→ img_encoding()
→ add_noise() / get_schedule()
→ atten_forward()
→ MMAttention
→ linear action head
```

推理链：

```text
deploy_h1.py / eval_libero.py
→ inference.modeling()
→ inference.predict_action()
→ preprocess()
→ model(..., training=False)
→ postprocess()
→ action chunk
```

实机链：

```text
deploy_h1.py
→ ImageClient.get_head_frame()
→ H1_2_ArmController.get_current_dual_arm_q()
→ Inspire_Controller_FTP shared arrays
→ DynamixelAgent.get_joint_state()
→ predict_action()
→ H1_2_ArmIK.solve_tau()
→ arm_ctrl.ctrl_dual_arm()
→ dual_hand_action_array[:]
→ active_cam.command_joint_state()
```

LIBERO 链：

```text
eval_libero.py
→ benchmark.get_benchmark_dict()
→ OffScreenRenderEnv
→ env.set_init_state()
→ predict_action()
→ env.step()
→ save_rollout_video()
```

## 12. 新手阅读顺序建议

### 第一阶段：能跑起来

目标：安装、准备数据、启动训练。

重点读：

- `README.MD`
- `requirements.txt`
- `config/deco.yaml`
- `train.py`
- `dataset.py`

### 第二阶段：能看懂

目标：理解模型和数据流。

重点读：

- `inference.py`
- `models/deco/deco.py`
- `models/deco/train_one_epoch.py`
- `models/act/ACT.py`
- `models/dp/diffusion_policy.py`

### 第三阶段：能修改和部署

目标：换数据、换模型、上实机。

重点读：

- `utils/cal_mean_std.py`
- `deploy/deploy_h1.py`
- `deploy/robot_control/robot_arm.py`
- `deploy/robot_control/robot_hand_inspire_deploy.py`
- `deploy/teleimager/cam_config_server.yaml`

## 13. 常见问题与排查

| 问题现象 | 可能原因 | 排查方法 | 解决建议 |
| -- | -- | -- | -- |
| `--batch_size` 不识别 | 代码参数是 `--batch-size` | 看 `train.py` argparse | 用 `--batch-size` |
| `--distributed False` 仍走 DDP | `type=bool` 解析问题 | 打印 `opt.distributed` | 用 `torchrun`，或后续修改 argparse |
| 单卡 python 训练报 dist 未初始化 | train/val 内直接 `dist.barrier()` | 看堆栈 | 用 `torchrun --nproc_per_node 1` |
| ACT 非 AMP 报 `action_size` | `opt.action_size` 不存在 | 看 `models/act/train_one_epoch.py` | 开 AMP 或修改为 `act_dim` |
| 找不到数据 | 数据结构/路径不符合 | 检查 `colors`、`tactiles`、`data.pkl` | 按 README 结构整理 |
| 无触觉数据报错 | `dataset.py` 总是加载 `.npy` | 看 `dataset.py` 触觉加载逻辑 | 提供 dummy 或改数据集逻辑 |
| 维度不匹配 | action_dim/statistics 不一致 | 打印 batch shape | 重算统计并同步 YAML |
| 权重加载失败 | YAML 模型结构和 checkpoint 不一致 | 看 load_state_dict 错误 | 用训练时同一 YAML |
| LIBERO 找不到路径 | `eval_libero.py` 硬编码 `/root/...` | 打开 Config | 改成当前机器路径 |
| 部署找不到 YAML | 从 `deploy/` 目录运行时 `./config` 不存在 | 看当前工作目录 | 用 `--yaml ../config/deco.yaml` 或从项目根运行 |
| 默认 `json_path` 找不到 | 代码不展开 `~` | `os.path.exists` 检查 | 传绝对路径 |
| 相机无图 | ZMQ IP/端口或服务器没起 | 先跑 `teleimager-client` | 改 IP/端口/配置 |
| Unitree 不响应 | 网卡错或 DDS topic 没数据 | 检查 `--network-interface` | 选连接机器人的网卡 |
| Dynamixel 失败 | `/dev/ttyUSB0` 错或无权限 | `ls -l /dev/ttyUSB*` | 改端口、加入 dialout |
| 实机动作异常 | 动作顺序/归一化错 | 打印 action 切片 | 做离线 replay 和小幅测试 |

## 14. 当前项目的不足与改进建议

工程上这个项目核心逻辑可读，但交接和复现还有不少需要补强的地方：

- README 与代码不一致：README 使用 `--batch_size`，代码只接受 `--batch-size`。
- 布尔 argparse 写法有风险：`--distributed False`、`--amp False` 实际会解析为 True。
- 非 DDP 路径不稳：训练/验证函数无条件 `dist.barrier()`。
- 多处路径硬编码：`utils/merge.py`、`convert_libero.py`、`task2json.py`、`eval_libero.py`。
- 部署默认路径不稳：`deploy_h1.py` 默认 YAML 和 `json_path` 容易失败。
- 设备参数分散：网卡、IP、串口、FPS、相机配置没有集中部署配置文件。
- 数据动作顺序需要明确文档化，尤其 JSON 初始化顺序和模型动作顺序不同。
- 缺少离线推理 CLI、数据检查脚本和最小 smoke test。
- 实机安全层不足：建议加入统一 action limit、关节范围、速度、workspace、急停状态检查。
- 依赖不完整：部署依赖没有统一 requirements/environment 文件。

建议优先改进：

1. 增加 `configs/deploy_h1.yaml`，集中放网卡/IP/串口/FPS/路径。
2. 修正 argparse 布尔参数。
3. 让 `dist.barrier()` 只在 `dist.is_initialized()` 时执行。
4. 增加 `scripts/check_dataset.py`。
5. 增加 `scripts/offline_infer.py`。
6. 给实机控制前增加动作 clip 和日志记录。
7. 把所有 `/root/...` 路径改成命令行参数。

## 15. 最终总结

这个项目做的是机器人双臂灵巧操作的模仿学习训练和部署。核心模型是 DECO，它把两路图像、本体状态、可选触觉和任务条件编码成多模态 token，通过 diffusion/flow-style 去噪方式预测未来动作序列。项目同时提供 ACT 和 Diffusion Policy baseline，也提供 LIBERO 仿真评估和 Unitree H1-2 实机部署链路。

想运行训练，最少需要准备 Python 3.10、PyTorch 2.6、CUDA、`requirements.txt` 依赖、符合 `dataset.py` 期望的数据结构，以及正确的 `config/deco.yaml` 统计量。训练推荐用 `torchrun`，输出主要在 `logs/` 下。

想跑推理，需要把训练好的 `best.pth` 写入 YAML 的 `model.pretrain_model_path`，再通过 `inference.predict_action()` 输入图像和状态，得到反归一化后的动作 chunk。

想部署实机，最关键的是四件事：图像服务器可用、Unitree DDS 网卡正确、Inspire Hand 和触觉 topic 正常、Dynamixel 串口和云台零点正确。上机前必须确认动作维度、动作顺序、归一化统计和安全限幅，否则风险很高。

想微调项目，优先从数据和配置开始：换数据后重算 `observation/action mean/std`，再调整 `chunk_size`、`lr`、`batch-size`、`inf_step`、`select-action`。模型结构微调主要看 `models/deco/deco.py` 和对应 YAML；实机微调主要看 `deploy_h1.py`、`robot_arm.py`、`robot_hand_inspire_deploy.py` 和 `teleimager` 配置。

本文档基于当前仓库文件和调用关系阅读整理，未修改项目代码，未实际启动训练或实机程序。
