# DECO 论文方法导读：需要补的知识与方法拆解

论文：**DECO: Decoupled Multimodal Diffusion Transformer for Bimanual Dexterous Manipulation with a Plugin Tactile Adapter**

这份笔记面向没有接触过机器人学习、扩散策略或多模态模型的读者。目标不是复述论文摘要，而是把论文的方法讲清楚：

- 论文到底要解决什么问题。
- DECO 每一步在做什么。
- 每一步为什么要这样做。
- 这些设计最后带来了什么效果。

## 1. 先用一句话理解这篇论文

DECO 是一个机器人动作生成模型。它让双臂灵巧手机器人同时使用 **视觉、本体感知和触觉** 来完成复杂操作任务。

可以先把它理解成下面这个系统：

```text
相机图像 + 机器人关节状态 + 手指触觉 + 任务类型
        ↓
      DECO 模型
        ↓
机器人未来一小段动作
```

这篇论文的核心不是“用了更多传感器”，而是：

> 不同传感器提供的信息类型不同，所以不能简单拼在一起。视觉、本体感知和触觉应该用不同方式进入模型。

## 2. 论文要解决的问题

机器人做普通抓取时，视觉通常已经很有用。例如看到苹果在哪里，控制手过去抓起来。

但在很多真实操作任务中，视觉会遇到明显限制：

- 手指挡住物体，导致关键接触点看不见。
- 图像看起来已经接触，但实际上没有压紧。
- 物体是否插入、扣合、滑动，常常不能只靠图像判断。
- 双臂灵巧手自由度高，动作空间大，错误会累积。

论文中的典型任务包括：

- 把物体放到盘子上。
- 从传送带抓取零件并分类。
- 打开垃圾桶盖、丢垃圾、再关上盖子。
- 双手分别拿插头和插座完成装配。

其中前两类任务主要依赖视觉和关节状态。后两类任务是 **接触密集任务**，需要知道是否碰到、是否压够、是否插到位，因此触觉非常重要。

论文要回答的实际问题是：

1. 所有任务都需要触觉吗？
2. 触觉能不能提升已有视觉策略？
3. 触觉应该怎样融合进模型，而不是简单拼接？
4. 能不能低成本给已有模型增加触觉能力？

## 3. 读懂论文需要的最小知识

### 3.1 模仿学习

这篇论文属于机器人模仿学习。

模仿学习的基本流程是：

```text
人遥操作机器人完成任务
        ↓
记录观测和动作
        ↓
训练模型学习“看到什么就该怎么动”
        ↓
部署到真实机器人上执行
```

几个关键词：

| 概念 | 含义 |
|---|---|
| observation | 当前观测，例如图像、关节状态、触觉 |
| action | 机器人要执行的动作 |
| trajectory | 一整段示范数据 |
| policy | 从观测到动作的模型 |
| action chunk | 一次预测未来多步动作 |

DECO 不是只预测下一步动作，而是预测未来一小段动作：

```text
A_t = [a_t, a_{t+1}, ..., a_{t+H-1}]
```

这样做的作用是让动作更连贯，减少每一步单独预测带来的抖动。

### 3.2 多模态学习

DECO 使用三类主要输入：

| 模态 | 在机器人中的含义 | 主要价值 |
|---|---|---|
| 视觉 | 双目相机图像 | 看物体位置、场景布局、目标状态 |
| 本体感知 | 双臂、双手、主动相机的关节状态 | 知道机器人自己当前姿态 |
| 触觉 | 手指和手掌上的压力/contact 信号 | 判断是否接触、压力是否足够、是否滑动 |

为什么不能简单拼接？

因为三类信息的性质不一样：

- 视觉是稠密的空间信息，几乎每一帧都有用。
- 本体感知是全局状态信息，描述机器人自己的身体配置。
- 触觉是稀疏的局部信息，通常只有接触瞬间最有用，而且有噪声。

如果把它们粗暴拼在一起，模型可能很难学会“什么时候该相信触觉，什么时候视觉已经够了”。

## 4. DECO 的整体方法流程

DECO 的方法可以拆成七步：

```text
1. 收集多模态机器人示范数据
2. 把当前观测整理成模型输入
3. 用 Flow Matching 生成未来动作序列
4. 用 MMDiT 处理视觉 token 和动作 token
5. 用 AdaLN 注入本体状态和任务条件
6. 用触觉 adapter 通过 cross-attention 注入触觉
7. 两阶段训练：先训练视觉策略，再低成本加入触觉
```

下面逐步解释每一步做什么，以及为什么这样做。

## 5. 第一步：收集 DECO-50 数据集

### 做了什么

作者采集了一个真实机器人数据集 DECO-50：

- 50 小时数据。
- 超过 500 万帧。
- 8000 多条成功轨迹。
- 包含双臂、灵巧手、主动双目视觉、触觉和本体感知。
- 覆盖 4 类场景、28 个子任务。

### 为什么要这样做

这篇论文研究的是双臂灵巧操作和触觉融合。现有数据集通常存在几个问题：

- 很多是单臂任务，不强调双手协作。
- 很多用简单夹爪，不是灵巧手。
- 很多没有触觉。
- 很多不是接触密集任务，无法体现触觉价值。

如果没有包含触觉和复杂接触的数据，就无法回答“触觉到底有没有用、该怎样用”。

### 这一步的作用

DECO-50 给模型提供了学习来源，也给实验提供了评测场景。它让作者可以比较：

- 只用视觉和本体感知的模型。
- 简单拼接触觉的模型。
- 使用 DECO 触觉 adapter 的模型。

## 6. 第二步：把机器人任务建模成动作序列生成

### 做了什么

DECO 的输入是当前时刻的多模态观测：

```text
O_t = [图像 I, 关节状态 q, 触觉 T, 任务条件 c]
```

输出是未来一段动作：

```text
A_t = [a_t, a_{t+1}, ..., a_{t+H-1}]
```

论文的目标是学习：

```text
p(A_t | O_t)
```

也就是给定当前观测，生成未来动作序列。

### 为什么要这样做

机器人操作不是一个静态分类问题，而是连续控制问题。模型不能只回答“这是苹果”，而是要生成一串能让机器人完成任务的动作。

一次预测未来多步动作有两个好处：

- 动作更平滑，因为模型能考虑短期未来。
- 执行更稳定，不需要每一帧都重新做完全独立的决策。

### 这一步的作用

这一步把机器人控制问题转成了序列生成问题。后面的 Diffusion Transformer 才有明确目标：生成未来动作 chunk。

## 7. 第三步：用 Flow Matching 生成动作

### 做了什么

DECO 不直接输出动作，而是使用 Flow Matching 生成动作序列。

可以把它理解成：

```text
随机噪声动作
    ↓
根据当前观测逐步修正
    ↓
合理的机器人动作序列
```

训练时：

1. 拿真实动作序列。
2. 给动作加噪声。
3. 让模型学习“从噪声动作走向真实动作”的方向。

推理时：

1. 从随机噪声动作开始。
2. 模型根据图像、关节状态、触觉等信息逐步更新。
3. 得到可执行的动作序列。

### 为什么要这样做

机器人动作可能不是唯一答案。例如抓一个物体，可以从左侧抓，也可以从右侧抓；装配时也可能有多种微调路径。

直接回归一个平均动作，可能会把多种可行动作平均成一个不合理动作。扩散或 Flow Matching 这类生成方法更适合表达多种可能动作。

### 这一步的作用

Flow Matching 负责“生成动作”。  
Transformer 负责“根据多模态输入判断该怎么生成”。

所以在 DECO 中，模型本质上是在预测一个速度场：如何把当前噪声动作推向正确动作。

## 8. 第四步：用 MMDiT 处理视觉和动作

MMDiT 是 **Multimodal Diffusion Transformer**，是 DECO 的核心模块。

### 做了什么

DECO 把图像和动作都变成 token：

- 图像通过 ResNet-34 编码成视觉 token。
- 带噪动作序列被编码成动作 token。
- 视觉 token 和动作 token 一起进入 self-attention。

直观结构是：

```text
双目图像 -> ResNet-34 -> 视觉 token
噪声动作 -> embedding -> 动作 token

视觉 token + 动作 token
        ↓
joint self-attention
        ↓
更新后的动作 token
        ↓
预测动作生成方向
```

### 为什么视觉要和动作做 joint self-attention

视觉是机器人操作中最主要的信息来源。机器人要知道物体在哪里、目标在哪里、手和物体的相对位置如何。

如果只把图像压缩成一个全局向量，很多空间细节会丢失。joint self-attention 允许动作 token 直接关注视觉 token，例如：

- 当前动作应该参考哪个物体。
- 手应该移动到图像中的哪个区域。
- 哪些视觉区域对下一步动作最重要。

### 这一步的作用

这一步让模型建立“看到什么”和“怎么动”之间的强关联。  
它是 DECO 比普通 Diffusion Policy 更强的关键之一。

## 9. 第五步：用 AdaLN 注入本体感知和任务条件

### 做了什么

本体感知包括：

- 双臂关节状态。
- 双手关节状态。
- 主动相机的关节状态。

任务条件是 one-hot，例如当前任务是抓苹果、关垃圾桶盖、装配某个零件等。

DECO 没有把这些信息简单拼到视觉 token 里，而是用 AdaLN 生成调制参数：

```text
本体状态 + 任务条件 + Flow Matching 时间步
        ↓
生成 scale / shift / gate
        ↓
调制 Transformer 中的视觉 token 和动作 token
```

### 为什么这样做

本体感知和视觉不一样。

视觉告诉模型外部世界是什么样。  
本体感知告诉模型机器人自己现在是什么姿态。

例如同样看到一个杯子：

- 如果手已经靠近杯子，下一步可能是闭合手指。
- 如果手还很远，下一步应该先移动手臂。

也就是说，本体状态更像一个全局条件，用来调节模型如何理解当前视觉和动作，而不是一个需要和图像像素逐点对齐的信息。

### 这一步的作用

AdaLN 让模型知道“机器人现在的身体状态”和“当前生成过程走到哪一步”。  
它控制网络内部的信息流，使动作生成更符合当前机器人姿态和任务阶段。

## 10. 第六步：用触觉 Adapter 注入触觉

这是论文最重要的设计之一。

### 做了什么

DECO 先训练一个视觉策略，然后给它加一个触觉 adapter。

触觉 adapter 包括：

1. 触觉 encoder。
2. 触觉 cross-attention 模块。
3. LoRA 低秩适配参数。

整体流程是：

```text
原始触觉读数
    ↓
触觉 encoder
    ↓
触觉 embedding
    ↓
cross-attention 注入预训练 DECO
    ↓
影响动作生成
```

### 触觉 encoder 做什么

触觉传感器分布在手指和手掌上。原始读数很多，而且有噪声。

作者用了两种方式提取触觉特征：

1. **区域平均**：对每个触觉 pad 的原始值求平均，得到区域级接触强度。
2. **可学习投影**：用线性层从原始触觉值中学习更细的模式。

然后把两类特征合并，并用 gating 机制筛选重要触觉信息。

### 为什么触觉要单独用 adapter

触觉有几个特点：

- 稀疏：大部分时间可能没有明显接触。
- 局部：只有少数手指或手掌区域有信号。
- 噪声大：传感器读数可能波动。
- 任务相关性强：只有在按压、插接、扣合时特别关键。

如果把触觉直接拼接到输入中，模型可能很难学会什么时候该用它，甚至可能被噪声干扰。

触觉 adapter 的好处是：

- 触觉作为专门通道进入模型。
- 模型可以通过 cross-attention 主动读取触觉。
- 不会破坏原来视觉策略的结构。
- 只需要训练少量新增参数。

### 为什么用 cross-attention

cross-attention 可以理解为：

> 当前动作生成过程主动查询触觉信息。

这比简单拼接更合理。因为触觉不是一直重要，而是在特定阶段突然变得很重要。

例如：

- 插头靠近插座时，需要触觉判断是否碰到边缘。
- 插入过程中，需要触觉判断是否卡住或滑动。
- 关垃圾桶盖时，需要触觉判断是否真的压下去并扣紧。

### 为什么用 LoRA

LoRA 的作用是低成本微调已有模型。

DECO.p 的做法是：

- 冻结原来的视觉 DECO。
- 只训练触觉 adapter 和少量 LoRA 参数。

这样可以避免从头训练整个模型。

### 这一步的作用

触觉 adapter 让已有视觉策略获得接触感知能力。  
它特别提升那些视觉看不清、必须依赖接触判断的任务。

## 11. 第七步：两阶段训练

### 做了什么

DECO 使用两阶段训练：

```text
阶段 1：训练视觉策略
输入：图像 + 本体状态 + 任务条件
输出：动作序列

阶段 2：冻结视觉策略，加入触觉 adapter
输入：图像 + 本体状态 + 任务条件 + 触觉
输出：动作序列
```

阶段 2 中，原始 DECO 大部分参数不更新，只训练少量 adapter 参数。

### 为什么不直接从头训练视觉加触觉模型

原因有三个：

1. 触觉数据成本高，从头训练代价大。
2. 视觉策略已经能解决很多任务，不应该轻易破坏。
3. 触觉只在部分任务和部分阶段关键，适合以插件形式加入。

### 这一步的作用

两阶段训练让 DECO 先学会“看着做”，再学会“摸着修正”。  
这符合机器人任务的实际结构：多数时候视觉主导，接触阶段触觉补充。

## 12. 方法整体总结

可以把 DECO 的每个模块和作用对应起来：

| 模块 | 做什么 | 为什么这样做 |
|---|---|---|
| Flow Matching | 从噪声中生成动作序列 | 动作可能有多种合理解，生成式方法更灵活 |
| ResNet-34 图像编码 | 把双目图像变成视觉 token | 保留空间视觉信息 |
| 视觉-动作 self-attention | 让动作 token 直接关注视觉 token | 建立“看到什么”和“怎么动”的关系 |
| AdaLN | 注入本体状态、任务条件、时间步 | 用全局状态调节网络，而不是粗暴拼接 |
| 触觉 encoder | 把原始触觉转成可用 embedding | 降噪、提取区域级和细粒度接触特征 |
| 触觉 cross-attention | 让模型按需查询触觉 | 触觉稀疏且阶段性重要，适合单独读取 |
| LoRA / adapter | 低成本加入触觉能力 | 冻结原视觉模型，避免全量重训 |
| 两阶段训练 | 先视觉，再触觉 | 保留视觉能力，同时补充接触感知 |

## 13. 论文为什么强调“解耦”

这里的“解耦”不是说三种模态完全不交流，而是说它们不要用同一种方式进入模型。

DECO 的解耦逻辑是：

```text
视觉：主信息源，进入 self-attention
本体感知：全局状态，进入 AdaLN
触觉：稀疏接触信号，进入 cross-attention adapter
```

为什么这很重要？

因为不同模态有不同角色：

- 视觉回答“外部世界在哪里”。
- 本体感知回答“机器人自己现在在哪里”。
- 触觉回答“有没有真实接触、力是否合适”。

如果全部拼接，模型需要自己从混乱输入中学出这些角色。  
DECO 通过结构设计直接告诉模型：这些信息应该以不同方式使用。

## 14. 实验结果说明了什么

论文比较了几类方法：

- **ACT**：经典模仿学习 baseline。
- **DP**：Diffusion Policy baseline。
- **DP.t**：把触觉简单拼接进 DP。
- **DECO**：不使用触觉的 DECO。
- **DECO.p**：使用插件触觉 adapter 的 DECO。

主结果：

| 方法 | Pick and Place | Material Sorting | Waste Disposal | Assembly | 全任务平均 | 接触任务平均 |
|---|---:|---:|---:|---:|---:|---:|
| ACT | 101/120 | 97/120 | 21/80 | 10/80 | 57.25% | 19.38% |
| DP | 93/120 | 67/120 | 30/80 | 15/80 | 51.25% | 28.13% |
| DP.t | 97/120 | 77/120 | 38/80 | 15/80 | 56.75% | 33.13% |
| DECO | 103/120 | 101/120 | 48/80 | 37/80 | 72.25% | 53.13% |
| DECO.p | 108/120 | 105/120 | 62/80 | 55/80 | 82.50% | 73.13% |

可以得到几个结论：

1. **DECO 的视觉策略本身就更强**  
   不用触觉时，DECO 已经明显强于 ACT 和 DP。

2. **触觉主要提升接触密集任务**  
   Pick and Place、Material Sorting 中，触觉提升有限。  
   Waste Disposal、Assembly 中，触觉提升明显。

3. **简单拼接触觉不够**  
   DP.t 在 Assembly 上没有超过 DP，说明触觉不是加进去就一定有用。

4. **DECO.p 的触觉 adapter 有效**  
   DECO.p 在接触任务平均成功率上从 53.13% 提升到 73.13%。

5. **低成本适配有效**  
   DECO.p 只训练 7.97M 参数，不到总参数的 10%，但效果接近从头训练的触觉模型。

## 15. 消融实验说明了什么

论文还比较了不同触觉注入方式：

| 方法 | 含义 |
|---|---|
| DECO | 不用触觉 |
| DECO.cs | 把触觉作为普通条件耦合注入 |
| DECO.ds | 触觉解耦注入，但从头训练 |
| DECO.p | 触觉 adapter 插件注入，只训练少量参数 |

结果显示：

- DECO.cs 只比 DECO 略好，说明简单注入触觉不够。
- DECO.ds 和 DECO.p 明显更好，说明解耦式触觉 cross-attention 有效。
- DECO.p 接近 DECO.ds，但训练参数少很多，说明 adapter 方式划算。

这部分实验主要证明：

> 论文的收益不是单纯来自“多用了触觉”，而是来自“用更合适的方式使用触觉”。

## 16. 最小理解版本

如果只想快速记住这篇论文，可以记住三句话：

1. DECO 是一个机器人动作生成模型，输入视觉、本体状态和触觉，输出未来动作序列。
2. 它把三种模态分开处理：视觉走 self-attention，本体状态走 AdaLN，触觉走 cross-attention adapter。
3. 触觉主要帮助接触密集任务，而且用 adapter 加触觉比简单拼接更有效、更省训练成本。

## 17. 推荐学习顺序

如果你想继续补基础，建议按这个顺序：

1. 模仿学习：policy、observation、action、trajectory。
2. 机器人操作：双臂、灵巧手、接触密集任务。
3. 多模态学习：视觉、本体感知、触觉分别是什么。
4. Transformer：token、self-attention、cross-attention。
5. Diffusion Policy / Flow Matching：为什么从噪声生成动作。
6. AdaLN：条件如何调节网络。
7. LoRA / Adapter：如何低成本给模型加新能力。
8. 消融实验：如何判断论文提出的模块是否真的有用。

## 18. 常见疑问补充

### 18.1 Flow Matching 是处理动作序列的常用方法吗？还有其他方法吗？

Flow Matching 不是机器人动作序列建模中唯一的方法，但它属于近几年越来越常见的一类 **生成式动作建模方法**。

机器人 policy 生成动作序列，常见路线大致有几类：

| 方法 | 基本想法 | 优点 | 局限 |
|---|---|---|---|
| 直接回归 | 输入观测，直接输出动作 | 简单、推理快 | 容易输出平均动作，难表达多种可行动作 |
| ACT 类方法 | 用 Transformer / CVAE 预测动作 chunk | 适合模仿学习，动作较平滑 | 对复杂多峰动作分布表达能力有限 |
| Diffusion Policy | 从噪声动作逐步去噪生成动作 | 能表达复杂、多模态动作分布 | 推理通常需要多步，计算更重 |
| Flow Matching | 学习从噪声到真实动作的连续变换方向 | 和 diffusion 类似，但训练/采样形式更直接 | 仍然需要多步推理，依赖模型设计 |
| 自回归策略 | 一步一步预测动作 | 形式直观，适合序列建模 | 长序列容易累积误差 |
| 强化学习策略 | 通过奖励学习动作 | 不一定依赖示范数据 | 真实机器人上采样成本很高 |

所以，Flow Matching 可以看作 Diffusion Policy 的近亲：二者都不是“一步直接输出动作”，而是从噪声逐步生成动作。

这篇论文选择 Flow Matching，主要是因为它适合生成连续动作序列，能处理“一个观测对应多种合理动作”的情况。

例如同样要抓一个物体，机器人可能有多种可行路径。如果直接回归，模型可能把多种路径平均成一条不自然的路径。生成式方法更适合表达这种多解性。

### 18.2 为什么说 DECO 是 diffusion transformer based policy？

DECO 可以拆成两部分理解：

```text
Diffusion / Flow Matching：负责“怎么生成动作”
Transformer：负责“怎么理解多模态输入并指导动作生成”
```

这里的 policy 指的是机器人策略，也就是：

```text
当前观测 -> 未来动作
```

DECO 不是普通神经网络 policy，而是一个基于生成模型的 policy。它从随机噪声动作开始，逐步生成未来动作序列。因此它属于 diffusion / flow matching 风格的 policy。

同时，DECO 的主干网络是 Transformer 结构，具体是 MMDiT，也就是 Multimodal Diffusion Transformer。所以可以说它是 diffusion transformer based policy。

### 18.3 Diffusion 和 Transformer 分别起什么作用？

可以用一个类比理解：

> Diffusion / Flow Matching 负责“把动作生成出来”，Transformer 负责“根据当前看到和感受到的信息，决定动作应该生成成什么样”。

更具体地说：

| 部分 | 作用 | 在 DECO 里处理什么 |
|---|---|---|
| Diffusion / Flow Matching | 动作生成机制 | 从噪声动作逐步变成真实动作 |
| Transformer | 条件建模和信息融合 | 处理视觉 token、动作 token、本体条件、触觉信息 |

Diffusion / Flow Matching 关心的是：

```text
当前这个噪声动作应该往哪个方向修改，才能更像真实可执行动作？
```

Transformer 关心的是：

```text
根据图像、关节状态、触觉和任务条件，应该如何决定这个修改方向？
```

因此，DECO 中真正输出的是动作生成过程中的速度或方向，而不是直接一次性输出最终动作。

### 18.4 为什么选择 diffusion transformer 架构？

主要有四个原因。

第一，机器人动作存在多解性。  
同一个任务可能有多种合理动作。如果直接回归，容易学出平均动作。Diffusion / Flow Matching 更适合表示这种多峰动作分布。

第二，动作 chunk 需要序列建模。  
DECO 一次预测未来多步动作，而 Transformer 很适合处理序列 token，包括动作 token。

第三，视觉和动作之间需要细粒度交互。  
Transformer 的 self-attention 可以让动作 token 直接关注图像 token。例如下一步动作可以关注插座边缘、手指位置、目标物体位置等。

第四，多模态条件需要灵活融合。  
DECO 要同时处理视觉、本体感知和触觉。Transformer 结构很适合通过不同路径注入不同模态，例如 self-attention、AdaLN、cross-attention。

所以这个架构的优势是：

- 能生成复杂连续动作。
- 能表达多个可能的动作方案。
- 能处理动作序列。
- 能让视觉和动作直接交互。
- 能自然扩展到多模态输入。
- 能通过 adapter 低成本加入触觉。

### 18.5 触觉 Adapter 是怎么影响整体 policy 的？

这个问题可以分成两个层次看：训练时怎么接入，推理时怎么影响动作。

#### 训练时

DECO 先训练一个不使用触觉的视觉策略：

```text
图像 + 本体状态 + 任务条件
        ↓
视觉 DECO policy
        ↓
动作序列
```

然后第二阶段冻结这个视觉策略，加入触觉 adapter：

```text
触觉读数
   ↓
触觉 encoder
   ↓
触觉 embedding
   ↓
cross-attention / LoRA adapter
   ↓
影响 DECO 中的动作生成特征
```

也就是说，触觉 adapter 不是替换原来的 policy，而是在原来的视觉 policy 上增加一条触觉信息通路。

#### 推理时

推理时，DECO 仍然在从噪声生成动作。每一步生成动作时，模型会同时看：

- 图像 token：外部场景是什么样。
- 动作 token：当前正在生成的动作长什么样。
- 本体条件：机器人当前身体姿态。
- 触觉 embedding：当前是否接触、压力是否合适、是否滑动。

触觉 adapter 通过 cross-attention 影响动作 token 的更新。也就是说，动作生成过程可以主动读取触觉信息。

例如装配任务中：

```text
没有触觉：
模型看到手和插座大概对齐，可能继续往前推。

有触觉：
模型感受到侧向接触或卡住，可能调整角度，而不是继续硬推。
```

再比如关垃圾桶盖：

```text
没有触觉：
图像上看起来盖子接近闭合，模型可能提前收手。

有触觉：
模型感受到按钮/盖子没有压到底，会继续施加动作直到扣紧。
```

所以触觉 adapter 对 policy 的影响不是“单独输出动作”，而是：

> 在动作生成的中间层提供接触信息，改变动作 token 的更新方向，最终让生成出来的动作更符合真实接触状态。

### 18.6 为什么不直接把触觉拼到输入里？

因为简单拼接会把所有信息混在一起，让模型自己去猜触觉什么时候重要。

触觉和视觉不同：

- 视觉几乎每一帧都有结构化信息。
- 触觉很多时候接近空信号。
- 触觉只在接触瞬间非常关键。
- 触觉读数可能有噪声。

如果直接拼接，模型可能出现两种问题：

1. 忽略触觉，因为大部分时间触觉不明显。
2. 被触觉噪声干扰，影响原本稳定的视觉策略。

使用 adapter 和 cross-attention 的好处是：

- 触觉有自己的编码器。
- 触觉通过单独路径进入模型。
- 动作生成过程可以按需读取触觉。
- 原来的视觉策略基本保留。
- 训练参数少，迁移成本低。

这就是论文中 “plugin tactile adapter” 的核心价值。
